import { View, TouchableHighlight, TouchableWithoutFeedback } from 'react-native';

const style = {

};

const CheckBox = () => (
  <View>
    <TouchableHighlight>
      <View />
    </TouchableHighlight>
  </View>
);

export default CheckBox;